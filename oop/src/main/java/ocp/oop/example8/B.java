package ocp.oop.example8;

public class B extends A   {

  /**
   *
   * class B inherit from A whatever is accessible = depending on the access mode (private, package, protected, public) =>
   * something maybe inherited or not.
   * in our case int x, and method m(), both are accessible by B,
   * because the access mode is package and A, B are in the same package.
   *
   *
   */


}
